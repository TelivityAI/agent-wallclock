{
  "name": "@agent-wallclock/cli",
  "version": "0.1.0",
  "description": "Agent Wallclock CLI",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "wallclock": "./dist/bin.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test --test-reporter=spec dist/*.test.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@agent-wallclock/core": "*"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
